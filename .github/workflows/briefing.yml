name: Daily Investor Briefing

on:
  schedule:
    # 早报: UTC 00:30 = 北京时间 08:30
    - cron: '30 0 * * *'
    # 晚报: UTC 07:30 = 北京时间 15:30
    - cron: '30 7 * * *'
  workflow_dispatch:
    inputs:
      type:
        description: 'Briefing type: morning or afternoon'
        required: true
        default: 'morning'

jobs:
  morning:
    if: github.event.inputs.type == 'morning' || (github.event_name == 'schedule' && github.event.schedule == '30 0 * * *')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests APScheduler
      - run: python main.py now morning
        env:
          SERVERCHAN_SENDKEY: ${{ secrets.SERVERCHAN_SENDKEY }}

  afternoon:
    if: github.event.inputs.type == 'afternoon' || (github.event_name == 'schedule' && github.event.schedule == '30 7 * * *')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: pip install requests APScheduler
      - run: python main.py now afternoon
        env:
          SERVERCHAN_SENDKEY: ${{ secrets.SERVERCHAN_SENDKEY }}
